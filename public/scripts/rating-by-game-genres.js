export default async (data) => {

    // Define dimension and SVG container
    const width = 1250
    const height = 1050;
    // const svg = d3.select('#scene').attr("width", width).attr("height", height);


    // Build the graph


}